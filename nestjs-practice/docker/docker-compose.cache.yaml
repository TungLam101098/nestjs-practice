services:
  redis:
    container_name: nest_redis
    image: redis:7.4.2-alpine
    volumes:
      - redis_data:/data
    restart: always
    networks:
      - nest-network

networks:
  nest-network:

volumes:
  redis_data:
