/**
 * Interface for defining dynamic search criteria fields
 * Represents a flexible key-value structure for search parameters
 */
export interface SearchFields {
  [key: string]: string | undefined;
}
