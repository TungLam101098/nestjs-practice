/**
 * Enum representing sorting order options for query results
 * Used to specify the direction of sorting in database queries and API endpoints
 */
export enum SortOrder {
  Asc = 'asc',
  Desc = 'desc',
}
