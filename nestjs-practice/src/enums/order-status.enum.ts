/**
 * Enum representing possible states of an order in the application
 * Defines the possible values for the status field of an order
 */
export enum OrderStatus {
  Pending = 'pending',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
