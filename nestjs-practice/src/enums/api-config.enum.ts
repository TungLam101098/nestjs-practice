/**
 * API Configuration Enumeration
 * Contains global API routing and versioning configuration
 * Used to maintain consistent API URL structure across the application
 */
export enum ApiConfig {
  Prefix = 'api',
  V1 = 'v1',
}
